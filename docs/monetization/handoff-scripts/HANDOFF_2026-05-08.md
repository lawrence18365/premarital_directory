# Revival Handoff — 2026-05-08

Everything I did in parallel today, what's left for you, and the order to do it in.

---

## TL;DR — the real situation

Your project is not dead. It has a pulse. Three things were silently killing revenue:

1. **Lead form was server-error'ing on every submit** — `process-lead-submission/index.ts` had a duplicate `const coupleName` declaration introduced in commit `af2fed7` (the "3-layer spam protection" commit). TypeScript/Deno can't compile that. **Fixed in this session.**
2. **Outreach cron has been failing for 2+ weeks** because of GitHub Actions billing. Five-minute fix on your end.
3. **No payment system.** Stripe is in `package.json` and used in zero lines of code. Need 3 Stripe Payment Links.

Your warm list is bigger than 22:
- **42 claimed counselors** — your warmest possible audience.
- **172 unclaimed scraped profiles with valid emails** — cold, but already on your site.
- **300 couple subscribers** — your audience.
- **396 profile clicks last 30 days** — real demand.

---

## What I did (committed to disk)

### 1. Fixed the lead form bug ✅

**File:** `supabase/functions/process-lead-submission/index.ts`

Removed the duplicate `const coupleName` declaration. Now only declared once, with `&` separator that works for both spam-check and DB insert.

**You need to:** deploy this edge function. From `/Users/lawrence/Desktop/premarital_directory`:
```bash
supabase functions deploy process-lead-submission
```
Verify: submit a real lead through any profile page; check that a row appears in `profile_leads` table.

### 2. Extracted real outreach lists ✅

**Files:**
- `docs/monetization/handoff-scripts/claimed-counselors.csv` — 42 rows, your warm list
- `docs/monetization/handoff-scripts/unclaimed-with-email.csv` — 172 rows, cold list with state breakdown

**State breakdown for cold list (top targets):** FL 14, GA 11, KS 10, PA 10, KY/NY/TN/WA 9 each.

### 3. Drafted both email templates ✅

**Files:**
- `docs/monetization/handoff-scripts/email-1-claimed-warm-list.md` — manual send to 42 claimed
- `docs/monetization/handoff-scripts/email-2-unclaimed-cold.md` — bulk send via cron to 172 unclaimed
- `~/Desktop/email/PAID_TEMPLATE_V1.md` — paste-ready Python `EMAIL_SEQUENCES` for the outreach repo

### 4. Wrote the user-count audit script ✅

**File:** `scripts/count-real-users.js`

Run any time to see live Supabase numbers:
```bash
node scripts/count-real-users.js
```

### 5. Wrote the list-extract script ✅

**File:** `scripts/extract-outreach-lists.js`

Re-run any time to refresh the warm/cold CSVs.

---

## What you need to do — ranked by leverage

### TODAY / THIS WEEKEND (hours, not days)

**1. Fix GitHub Actions billing (5 min).** Settings → Billing → update payment method on the `lawrence18365` account. Verify the next scheduled run on `email-cron.yml` succeeds.

**2. Deploy the lead form fix (10 min).**
```bash
cd /Users/lawrence/Desktop/premarital_directory
supabase functions deploy process-lead-submission
```
Then test by submitting a real inquiry through `/premarital-counseling/ny/new-york/robin-a-bryant-phd-pc` and verifying a row appears in `profile_leads`.

**3. Test signup flow end-to-end (30 min).** Pretend to be a new counselor. Sign up with a fresh email. Click confirmation. Try to create a profile. Whatever breaks, that's the next bug. Dr. Russo and others got stuck here. Don't skip this — the warm-list email apologizes for "form bug fixed today" and you don't want them to hit a *second* bug.

**4. Create 3 Stripe Payment Links (15 min).** $79 / $149 / $299. See `~/Desktop/email/PAID_TEMPLATE_V1.md` § Stripe Payment Link setup for exact config.

### MONDAY

**5. Send the warm-list email manually to all 42.** Use `email-1-claimed-warm-list.md` as the template. Paste each name, profile URL, and the $79 Stripe link by hand. ~1 hour. **This is your highest-leverage hour of the week.**

### TUESDAY

**6. Swap the paid template into the outreach repo and re-run setup.**
```bash
cd ~/Desktop/email
# Edit setup_wedding_counselors.py — paste sequences from PAID_TEMPLATE_V1.md
python setup_wedding_counselors.py
# Verify sequences updated in Turso via app.py admin UI
```
Confirm cron is green again (GitHub Actions billing must be fixed first).

### WED–FRI

**7. Cron does the work.** 50/day × 5 days × 3 emails = 750 sends across 172 unclaimed (with follow-ups). Watch inbox for replies.

**8. Reply manually to anyone interested with the $79 Stripe link.** Same-day replies. Don't make them wait.

### SUNDAY MAY 17 — MOMENT OF TRUTH

Check `professional_subscriptions` table (or your Stripe dashboard):

- **0 paid:** offer is wrong. Drop to $29/mo or "$19 first month, $9/mo after." Try again.
- **1 paid:** real signal. Build the Stripe webhook (instructions in PAID_TEMPLATE_V1.md).
- **3+ paid:** you have a small business. Now go fix the website (real prerender, soft-404s, etc.).

---

## Things I deliberately did NOT do (need your call)

- **Did not deploy the edge function.** It needs your `supabase functions deploy` push.
- **Did not change the outreach template in code.** I wrote it as Markdown so you can review before swap. Don't want stale free-pitch sending while you're still fixing billing.
- **Did not send any emails.** Drafts only.
- **Did not create Stripe Payment Links.** Needs your Stripe login.
- **Did not touch GitHub Actions billing.** Needs your account.

---

## What I still want to look at if you want me to (not done today)

- **Trace the signup → profile creation flow** for the bugs Dr. Russo, Hannah Boggs, Pamela hit. They're probably still there. Worth doing before sending the warm-list email so people don't bounce on the SAME bugs that frustrated them in February.
- **Check `couple_subscribers` mailing list** — 300 couples who gave you their email. Worth a "we're back, here's what's new" email? Or a referral mechanic ("forward to a counselor friend, get a free coaching session")?
- **Audit the `claim-profile` flow.** Whether claim tokens still work for the 1,413 unclaimed profiles, or expired.

Just say "do X" and I'll go.

---

## Tasks I closed this session

- ✅ Find outreach repo state and last send date — found at `~/Desktop/email`, last sent ~Apr 22, cron failing on billing since
- ✅ Check Spacemail inbox for replies — 48 responses logged in `archive/crm_responses_full.txt`, 22+ explicitly interested
- ✅ Diagnose lead form bug — duplicate const declaration in `process-lead-submission/index.ts`, FIXED
- ✅ Extract 42 claimed counselors → `claimed-counselors.csv`
- ✅ Extract 172 unclaimed-with-email → `unclaimed-with-email.csv`
- ✅ Draft warm-list email → `email-1-claimed-warm-list.md`
- ✅ Draft cold-outreach template → `email-2-unclaimed-cold.md` + `~/Desktop/email/PAID_TEMPLATE_V1.md`
